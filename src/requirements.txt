edge-tts>=6.1.9
# optional / recommended runtime helpers
pydub>=0.25.1
tqdm>=4.65.0
pyperclip>=1.8.2
# ffmpeg must be installed on the system (not a pip package)

# Test requirements
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
coverage[toml]>=7.0.0

# Development / linting
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
